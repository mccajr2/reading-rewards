spring:
  config:
    import: optional:file:.env
  datasource:
    url: jdbc:postgresql://localhost:5432/reading
    username: reading
    password: reading
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  mail:
    host: smtp.mailtrap.io
    port: 2525
    username: 63b2890fe73690
    password: 56c1f4149cf82b

server:
  port: 8080

frontend:
  url: http://localhost:3000
