spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reading
    username: reading
    password: reading
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080
